[project]
name = "playwright-python"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "page>=0.3",
    "playwright>=1.60.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-playwright>=0.7.1",
    "setuptools<81",
]
